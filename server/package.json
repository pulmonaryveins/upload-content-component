{
  "name": "upload-proxy-server",
  "version": "1.0.0",
  "description": "Backend proxy — keeps Transloadit and Google credentials off the browser",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.0",
    "express": "^4.18.0"
  }
}
